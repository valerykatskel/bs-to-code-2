const code = document.getElementById("bsCode");
const codeFormatted = document.getElementById("bsCodeFormatted");
const appCode = document.getElementsByClassName("app-code")[0];

export { code, codeFormatted, appCode };
